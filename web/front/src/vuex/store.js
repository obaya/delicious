import Vue from 'vue'
import Vuex from 'vuex'

// 引入需要用到vuex的文件
// import login from '../components/login.js'

Vue.use(Vuex)

export default new Vuex.Store({
    modules: {
        // login,
    }
})
